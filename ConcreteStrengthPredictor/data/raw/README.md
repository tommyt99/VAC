# Raw data

Raw downloads are intentionally not committed. Fetch the DOI-pinned UCI benchmark with:

```bash
concrete-strength fetch-data
```

The command verifies the archive SHA-256 before extraction. You can also train directly from the
original `Concrete_Data.xlsx` workbook by passing `--data /path/to/Concrete_Data.xlsx`.

